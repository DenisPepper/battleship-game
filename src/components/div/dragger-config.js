export const DRAGGER_HEIGHT = 600;

export const DRAGGER_WIDTH = 400;

export const INNER_THICKNESS = 16;

export const INITIAL_ID = 1;

export const INITIAL_AREA = {
  id: INITIAL_ID,
  top: 0,
  left: 0,
  width: DRAGGER_WIDTH,
  height: DRAGGER_HEIGHT,
  removeSelection: null,
  border: {
    top: null,
    right: null,
    bottom: null,
    left: null,
  },
};
