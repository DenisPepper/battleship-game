import './home.css';

export function Home() {
  return (
    <canvas className='canvas-container' width={500} height={500}>
      A visual design tool for the interior filling of the cabinet.
    </canvas>
  );
}
